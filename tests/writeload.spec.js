// @ts-check
const { test, expect } = require('@playwright/test');

test('writer user', async ({ page }) => {
  await page.goto('https://public.tableau.com/app');

  // register user

  // login

  // web-create a viz

  // from the home page, follow some users

  // from the home page, favorite some vizzes

  // go to own profile, and edit some settings (NOT NEEDED)

  // go to own account settings, and edit some settings (NOT NEEDED)
});
